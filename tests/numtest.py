import inspect
class numtest:
    def retNum(self):
        return 1
    def executeTestCasesInMyClass(self):
        print('a')
        # TODO:以下のコードは表示結果が得られ次第すぐに削除する
        #for tmpStr in inspect.getmembers(self,inspect.ismethod):
        #    print(tmpStr[0])
        # ここまで
